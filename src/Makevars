# We need to know where libRcpp.so and Rcpp.h reside
# Protobuf headers and library are needed too
PKG_CPPFLAGS = $(shell Rscript -e 'Rcpp:::CxxFlags()')
PKG_LIBS = $(shell Rscript -e 'Rcpp:::LdFlags()') -lprotobuf
